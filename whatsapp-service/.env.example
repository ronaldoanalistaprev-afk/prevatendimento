# URL do app Next (rota de ingestão). Em dev normalmente:
WEB_URL=http://localhost:3000

# MESMO valor do INGEST_SECRET usado no web/.env.local
INGEST_SECRET=troque-por-um-segredo-aleatorio-longo

# Porta em que este serviço expõe o endpoint /send (o web chama aqui para enviar)
PORT=8088
